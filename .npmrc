# HeroUI 2.x declares a Tailwind v4 peer, but the Nuki stack (mirroring
# nk-admin-portal) runs it on Tailwind v3.4. Relax peer resolution so installs
# are reproducible with that known-good combination.
legacy-peer-deps=true
